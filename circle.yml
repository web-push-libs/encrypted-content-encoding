machine:
  node:
    version: v6.1.0
  python:
    version: 3.5.2

dependencies:
  cache_directories:
    - nodejs/node_modules
  pre:
    - cd nodejs;npm install
    - pip install tox

test:
  override:
    - cd nodejs;node test.js
    - cd python;tox
